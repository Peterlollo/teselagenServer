//Be sure to rename all angular modules, rename file structure, and reference the proper directories & files in the index.hmtl page
angular.module('mean.services', [])
  .factory('SomeFactory', function() {
    //inject dependencies as arguments to function
    //declare helper functions/services
    //return an object with the functions as key-value pairs
  });